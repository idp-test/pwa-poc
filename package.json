{
  "name": "pwa-poc",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@10.9.7",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "generate-icons": "node scripts/generate-icons.js"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "turbo": "^2.5.0"
  }
}
